23:      if (userDoc.exists() && userDoc.data().apiKey) {
24:        userApiKey = userDoc.data().apiKey;
102:      const credits = parseFloat(String(userDoc.data().credits || 0));
122:        const credits = parseFloat(String(userDoc.data().credits || 0));
307:        const credits = parseFloat(String(userDoc.data().credits || 0));
907:            ...doc.data()
1340:                const base64EncodeString = part.inlineData.data;
1402:    const files = Array.from(e.dataTransfer.files) as File[];
1500:    const files = Array.from(e.dataTransfer.files) as File[];
2805:            ...doc.data()
3042:              const base64EncodeString = part.inlineData.data;
4116:          const base64EncodeString = part.inlineData.data;
4431:        ...(doc.data() as any)
4461:    if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
4462:      const file = e.dataTransfer.files[0];
4564:    if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
4565:      Array.from(e.dataTransfer.files).forEach(file => {
4591:    if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
4592:      const file = e.dataTransfer.files[0];
4672:          const base64EncodeString = part.inlineData.data;
4816:                base64: part.inlineData.data,
5074:          const b64 = part.inlineData.data;
6626:            generatedImageUrl = `data:${part.inlineData.mimeType || 'image/png'};base64,${part.inlineData.data}`;
6693:    const file = e.dataTransfer.files?.[0];
8723:    const file = e.dataTransfer.files?.[0];
8835:            generatedImageUrl = `data:${part.inlineData.mimeType};base64,${part.inlineData.data}`;
9850:              if (part.inlineData?.data) {
9851:                base64 = part.inlineData.data;
9878:            if (part.inlineData?.data) {
9879:              base64 = part.inlineData.data;
10171:          const timeA = a.data().createdAt?.toMillis?.() || 0;
10172:          const timeB = b.data().createdAt?.toMillis?.() || 0;
10177:          const data = doc.data();
